#include "main.h"
#include <stdio.h>

/**
* _strlen - returns the lenght of string
*@s : Pointer to the variable holding the string
* Return: void
*/

int _strlen(char *s)
{
	char *s = str;
	int lenght;

	lenght = _strlen(str);
}
