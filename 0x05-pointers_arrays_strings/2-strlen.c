#include "main.h"
#include <stdio.h>

/**
* _strlen - returns the lenght of string
*@s : Pointer to the variable holding the string
* Return: void
*/

int _strlen(char *s)
{
	int lenght = 0;

	while (s, lenght != '\0')
	lenght++;

	return (lenght);
}
